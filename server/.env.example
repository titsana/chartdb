PORT=3001
DATABASE_URL=postgres://chartdb:chartdb@localhost:5432/chartdb
CORS_ORIGIN=http://localhost:5173

# Optional: set both to require Azure AD sign-in for all API requests.
# Leave both unset to run with no authentication (default).
AZURE_AD_CLIENT_ID=
AZURE_AD_TENANT_ID=
